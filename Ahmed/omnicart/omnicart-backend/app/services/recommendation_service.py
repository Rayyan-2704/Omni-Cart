# recommendation_service.py — SVD + cosine similarity (Step 17)
# Connected to ML models in Phase 3

def get_recommendations_for_customer(customer_id: int, top_n: int = 5):
    """
    Returns top N recommended product IDs for a customer.
    Phase 3 will populate this with SVD collaborative filtering.
    """
    # Placeholder — returns empty list until ML models are trained
    return []
