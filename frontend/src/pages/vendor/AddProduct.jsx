import { useState, useEffect } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { ArrowLeft, Save, Sparkles, Package, Terminal, Box, Database } from 'lucide-react';
import API from '../../api/axios';
import BorderGlow from '../../components/ui/BorderGlow';
import toast from 'react-hot-toast';

export default function AddProduct() {
  const navigate = useNavigate();
  const [loading, setLoading] = useState(false);
  const [genLoading, setGenLoading] = useState(false);
  const [form, setForm] = useState({ name: '', description: '', price: '', stock_qty: '', brand: '', category_id: '' });
  const [categories, setCategories] = useState([]);

  useEffect(() => {
    API.get('/categories')
      .then(res => setCategories(res.data.categories))
      .catch(() => toast.error('Failed to load categories'));
  }, []);

  const update = (k, v) => setForm(p => ({ ...p, [k]: v }));

  const handleGenerate = async () => {
    if (!form.name || !form.category_id) return toast.error('Enter name & category first');
    setGenLoading(true);
    try {
      const res = await API.post('/products/generate-description', {
        name: form.name, category_id: parseInt(form.category_id), price: parseFloat(form.price || 0), brand: form.brand,
      });
      update('description', res.data.description);
      toast.success('Description generated by AI!');
    } catch { toast.error('AI generation failed. Write manually.'); }
    finally { setGenLoading(false); }
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    if (!form.category_id) return toast.error('Please select a category');
    setLoading(true);
    try {
      await API.post('/vendor/products', { ...form, price: parseFloat(form.price), stock_qty: parseInt(form.stock_qty || 0), category_id: parseInt(form.category_id) });
      toast.success('Product created!');
      navigate('/vendor/products');
    } catch (err) { toast.error(err.response?.data?.error || 'Failed'); }
    finally { setLoading(false); }
  };

  return (
    <div className="section-padding py-16 sm:py-24 max-w-4xl mx-auto">
      <Link to="/vendor/products" className="inline-flex items-center gap-3 text-[10px] font-black uppercase tracking-[0.2em] text-surface-400 hover:text-primary-500 mb-12 transition-colors group">
        <ArrowLeft className="w-4 h-4 group-hover:-translate-x-1 transition-transform" /> Back to Repository
      </Link>
      
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}>
        <header className="mb-12">
          <span className="text-[10px] font-black uppercase tracking-[0.4em] text-primary-500 mb-4 block">Deployment Console</span>
          <h1 className="text-5xl sm:text-6xl font-black text-surface-950 dark:text-white mb-4 tracking-tighter">New <span className="text-surface-400">Hardware.</span></h1>
          <p className="text-surface-500 font-medium text-lg">Initialize a new product entry in the global marketplace catalog.</p>
        </header>

        <form onSubmit={handleSubmit} className="space-y-12">
          {/* General Information */}
          <BorderGlow className="w-full" borderRadius={48} glowRadius={10} animated={true}>
            <div className="p-10 sm:p-12 relative overflow-hidden w-full">
              <div className="absolute top-0 right-0 w-32 h-32 bg-primary-500/5 rounded-full blur-3xl -mr-16 -mt-16" />
              
              <div className="flex items-center gap-4 mb-10">
                <div className="w-10 h-10 rounded-xl bg-primary-500/10 flex items-center justify-center text-primary-500">
                  <Terminal className="w-5 h-5" />
                </div>
                <h3 className="text-xs font-black uppercase tracking-[0.2em] text-surface-950 dark:text-white">Product Initialization</h3>
              </div>

              <div className="space-y-8">
                <div className="group w-full">
                  <label className="block text-[10px] font-black uppercase tracking-[0.2em] text-surface-400 mb-3 group-focus-within:text-primary-500 transition-colors">Canonical Name *</label>
                  <input type="text" value={form.name} onChange={(e) => update('name', e.target.value)} 
                    placeholder="e.g. Sony WH-1000XM5" 
                    className="input-field !py-5 !px-8 !rounded-2xl border-surface-100 dark:border-white/5 bg-surface-50/50 dark:bg-white/[0.02] focus:ring-4 focus:ring-primary-500/10 w-full" required />
                </div>

                <div className="grid grid-cols-1 sm:grid-cols-2 gap-8 w-full">
                  <div className="group w-full">
                    <label className="block text-[10px] font-black uppercase tracking-[0.2em] text-surface-400 mb-3 group-focus-within:text-primary-500 transition-colors">Classification *</label>
                    <div className="relative w-full">
                      <select value={form.category_id} onChange={(e) => update('category_id', e.target.value)} 
                        className="input-field !py-5 !px-8 !rounded-2xl border-surface-100 dark:border-white/5 bg-surface-50/50 dark:bg-white/[0.02] appearance-none w-full" required>
                        <option value="">Select Sector</option>
                        {categories.map(c => <option key={c.category_id} value={c.category_id}>{c.name}</option>)}
                      </select>
                      <Box className="absolute right-6 top-1/2 -translate-y-1/2 w-4 h-4 text-surface-400 pointer-events-none" />
                    </div>
                  </div>
                  <div className="group w-full">
                    <label className="block text-[10px] font-black uppercase tracking-[0.2em] text-surface-400 mb-3 group-focus-within:text-primary-500 transition-colors">Brand Identity</label>
                    <input type="text" value={form.brand} onChange={(e) => update('brand', e.target.value)} 
                      placeholder="e.g. Sony" 
                      className="input-field !py-5 !px-8 !rounded-2xl border-surface-100 dark:border-white/5 bg-surface-50/50 dark:bg-white/[0.02] w-full" />
                  </div>
                </div>
              </div>
            </div>
          </BorderGlow>

          {/* Logistics & Value */}
          <BorderGlow className="w-full" borderRadius={48} glowRadius={10} animated={true}>
            <div className="p-10 sm:p-12 w-full">
              <div className="flex items-center gap-4 mb-10">
                <div className="w-10 h-10 rounded-xl bg-accent-500/10 flex items-center justify-center text-accent-500">
                  <Database className="w-5 h-5" />
                </div>
                <h3 className="text-xs font-black uppercase tracking-[0.2em] text-surface-950 dark:text-white">Logistics & Valuation</h3>
              </div>

              <div className="grid grid-cols-1 sm:grid-cols-2 gap-8 w-full">
                <div className="group w-full">
                  <label className="block text-[10px] font-black uppercase tracking-[0.2em] text-surface-400 mb-3 group-focus-within:text-accent-500 transition-colors">Deployment Value (PKR) *</label>
                  <div className="relative w-full">
                    <input type="number" value={form.price} onChange={(e) => update('price', e.target.value)} 
                      placeholder="49999" 
                      className="input-field !py-5 !px-8 !rounded-2xl border-surface-100 dark:border-white/5 bg-surface-50/50 dark:bg-white/[0.02] w-full" required min="1" />
                  </div>
                </div>
                <div className="group w-full">
                  <label className="block text-[10px] font-black uppercase tracking-[0.2em] text-surface-400 mb-3 group-focus-within:text-accent-500 transition-colors">Inventory Allocation</label>
                  <input type="number" value={form.stock_qty} onChange={(e) => update('stock_qty', e.target.value)} 
                    placeholder="50 Units" 
                    className="input-field !py-5 !px-8 !rounded-2xl border-surface-100 dark:border-white/5 bg-surface-50/50 dark:bg-white/[0.02] w-full" min="0" />
                </div>
              </div>
            </div>
          </BorderGlow>

          {/* Content & AI */}
          <BorderGlow className="w-full" borderRadius={48} glowRadius={10} animated={true}>
            <div className="p-10 sm:p-12 w-full flex flex-col">
              <div className="flex justify-between items-center mb-10 w-full">
                <div className="flex items-center gap-4">
                  <div className="w-10 h-10 rounded-xl bg-emerald-500/10 flex items-center justify-center text-emerald-500">
                    <Sparkles className="w-5 h-5" />
                  </div>
                  <h3 className="text-xs font-black uppercase tracking-[0.2em] text-surface-950 dark:text-white">Technical Specification</h3>
                </div>
                <button type="button" onClick={handleGenerate} disabled={genLoading} 
                  className="text-[10px] font-black uppercase tracking-[0.2em] text-primary-500 hover:text-primary-600 flex items-center gap-3 transition-all disabled:opacity-50">
                  {genLoading ? <div className="w-4 h-4 border-2 border-primary-500/30 border-t-primary-500 rounded-full animate-spin" /> : <Sparkles className="w-4 h-4" />}
                  Sync AI Generation
                </button>
              </div>
              
              <div className="w-full flex-1">
                <textarea value={form.description} onChange={(e) => update('description', e.target.value)} 
                  placeholder="Describe the hardware architecture..." 
                  className="input-field !py-8 !px-8 !rounded-[32px] min-h-[200px] resize-none border-surface-100 dark:border-white/5 bg-surface-50/50 dark:bg-white/[0.02] w-full" />
              </div>
            </div>
          </BorderGlow>

          <button type="submit" disabled={loading} 
            className="btn-primary w-full !py-6 !rounded-[24px] flex items-center justify-center gap-4 shadow-2xl shadow-primary-500/20 group relative overflow-hidden">
            {loading ? (
              <div className="w-6 h-6 border-2 border-white/30 border-t-white rounded-full animate-spin" />
            ) : (
              <>
                <Save className="w-5 h-5 group-hover:scale-110 transition-transform" /> 
                <span className="text-sm font-black uppercase tracking-[0.3em]">Initialize Deployment</span>
              </>
            )}
          </button>
        </form>
      </motion.div>
    </div>
  );
}
